package eg.edu.alexu.csd.filestructure.sort;

import java.util.ArrayList;

public class testing {

/*	public static void main(String[] args) {
	
		ArrayList<Integer> arr = new ArrayList();
     arr.add(8); arr.add(6); arr.add(3); arr.add(1); 

        Sort he = new Sort();
        
        he.sortFast(arr);
    for(int i=0;i<arr.size();i++) {
    	System.out.println(arr.get(i));
    }
	}
/*	public void quicksort(Object arr[],int l,int r) {
		if(l >= r) return;
		
		int p=getpivot(l, r, arr);
		quicksort(arr, l, p-1);
		quicksort(arr,p+1 , r);
	}
	private int  getpivot(int l,int r,Object []arr) {
		T pivot =(T) arr[l]; int p=l; 
		int i=l, j=r;
		while(i<j) {
			if(pivot.compareTo((T) arr[i]) == 0) {
				if(((T) arr[j]).compareTo(pivot) >=0) j--;
				else {
					swap(i,j,arr); p=j; i++;
				}
			}
		   else {
			   if(((T) arr[i]).compareTo(pivot) <=0) i++;
			   else {
				   swap(i,j,arr); p=i; j--;
			   }
		   }
		}	 
    return p;
	}    */

}
